import { SetNamesToColor } from 'components/SetNamesToColor';

const Color = (): JSX.Element => (
  <>
    <SetNamesToColor />
  </>
);

// eslint-disable-next-line import/no-default-export
export default Color;
